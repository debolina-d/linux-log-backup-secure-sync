## 📌 Project Overview
This project implements an automated Linux log backup and secure synchronization system using Bash scripting.  
System logs are archived with timestamps, compressed, and securely synchronized using SSH and rsync.

The automation is scheduled using cron to run daily during low-traffic hours, following real-world DevOps best practices.  
Localhost is used to simulate a remote server environment.
